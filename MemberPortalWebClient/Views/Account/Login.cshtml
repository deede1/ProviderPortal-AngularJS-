
@{
    ViewBag.Title = "Log in";
}
<div id="RegistrationApp" ng-app="Registration" ng-controller="LoginController">


DEPRACATED       DEPRACATED       DEPRACATED       DEPRACATED       DEPRACATED       
    DEPRACATED       DEPRACATED       DEPRACATED       DEPRACATED       DEPRACATED       

    <h2>@ViewBag.Title</h2>
    <div class="panel" style="border:1px solid red">
        <div class="col-md-8">
            <section id="loginForm">
                <form class="form-horizontal" role="form" name="login" novalidate="novalidate">
                    <h4>Sign in with your IEHP account.A</h4>
                    <hr />
                    <div class="form-group">
                        <label class="control-label" for="userName">Username</label>
                        <div class="col-md-10">
                            <input type="text" name="userName" class="form-control" ng-model="userName" />
                        </div>
                    </div>
                    <div class="form-group">
                        <label class="control-label" for="password">Password</label>
                        <div class="col-md-10">
                            <input type="password" name="password" class="form-control" ng-model="password" />
                        </div>
                    </div>
                    <div class="form-group">
                        <div class="col-md-offset-2 col-md-10">
                            <button class="btn btn-default" ng-click="submit()">Log in</button>
                        </div>
                    </div>
                </form>
                <p>
                    Register
                </p>
            </section>
        </div>
        <div class="col-md-4">

        </div>
    </div>
</div>
    @section Scripts {
        <script src="~/Scripts/App/RegistrationApp.js"></script>
    }
